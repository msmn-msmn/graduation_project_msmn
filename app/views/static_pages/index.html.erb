<div class="hero-section text-center py-5">
  <div class="container">
    <h1>Cat's Hand Schedule</h1>
    <p class="lead">タスク見積もりをお手伝い！</p>
    
    <% if user_signed_in? %>
      <%= link_to "タスク一覧へ", tasks_path, class: "btn btn-primary btn-lg" %>
    <% else %>
      <div class="mt-4">
        <%= link_to "新規登録", new_user_registration_path, class: "btn btn-primary btn-lg me-3" %>
        <%= link_to "ログイン", new_user_session_path, class: "btn btn-outline-primary btn-lg" %>
      </div>
    <% end %>
  </div>
</div>
